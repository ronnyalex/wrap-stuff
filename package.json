{
  "name": "log-wrap",
  "displayName": "log-wrap",
  "description": "wrapping string to log the output",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.86.0"
  },
  "repository": {
    "type" : "git",
    "url" : "https://github.com/ronnyalex/log-wrap.git"
  },
  "publisher": "itsg00d",
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "log.wrap",
        "title": "Log wrap name and value"
      },
      {
        "command": "log.wrap.name",
        "title": "log wrap name"
      }
  ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.86.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3",
    "@vscode/test-cli": "^0.0.4",
    "@vscode/test-electron": "^2.3.9"
  }
}
